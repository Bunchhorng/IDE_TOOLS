# ETEC STUDIO Frontend

Mobile-first Progressive Web App for the ETEC STUDIO online compiler. React 19 +
TypeScript + Vite + Tailwind CSS v4, Monaco editor, offline shell via Workbox.

## Quick start

```bash
npm install
npm run dev       # requires the backend stack (see repo README)
npm run build     # tsc -b && vite build
npm run lint      # oxlint
```

## Documentation

Full frontend guide (structure, routing, state, editor page, stylings, PWA):
see [`docs/frontend.md`](../docs/frontend.md) and the repo-level
[`README.md`](../README.md).